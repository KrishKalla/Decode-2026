package org.firstinspires.ftc.teamcode.utility.trajectory;
import org.firstinspires.ftc.teamcode.utility.trajectory.constantsTrajectory;
//TODO: spin decay + add varying v while keeping hoodLength constant
public class Solver {
    private static double v0;
    private static double h2;
    private static double d;
    Solver(double v0, double h2, double d) {
        setv0(v0);
        seth2(h2);
        setd(d);
    }

    double getv0(){
        return v0;
    }

    double getd() {
        return d;
    }

    double geth2(){
        return h2;
    }
    void setv0(double v0){
        this.v0 = v0;
    }

    void setd(double d) {
        this.d = d;
    }

    void seth2(double h2) {
        this.h2 = h2;
    }
    // bisection algo to get hoodLength
    static double solve(double low, double high) {
        double tol = 1e-5;

        double errLow = getHeightError(low);
        double errHigh = getHeightError(high);

        if (errLow * errHigh > 0) {
            return -67.0;
        }

        double mid = 0, errMid = 0;
        for (int i = 0; i < 100; i++) {
            mid = (low + high) / 2;
            errMid = getHeightError(mid);
            if (Math.abs(errMid) < tol) {
                break;
            }

            if (errLow * errMid < 0) {
                high = mid;
                errHigh = errMid;
            }

            else {
                low = mid;
                errLow = errMid;
            }
        }
        return mid;
    }

    static double getHeightError(double hoodLength) {
        // basically runs through three "phases" of the ball: running along the hood
        // the "transition" phase
        // the :flying through the air" phase
        double thetaExit = hoodLength / constantsTrajectory.rHood;
        double[] hoodState = {0.0, v0};
        double dt = 0.001;

        while(hoodState[0] < thetaExit) {
            rk4StepHood(hoodState, dt);
            if(hoodState[1] <= 0) {
                return -41.0;
            }
        }

        double vExit = hoodState[1];
        double x = constantsTrajectory.rHood * Math.sin(thetaExit);
        double y = constantsTrajectory.h1 + constantsTrajectory.rHood * (1 - Math.cos(thetaExit));
        double vx = vExit * Math.cos(thetaExit);
        double vy = vExit * Math.sin(thetaExit);

        double[] flightState = {x, y, vx, vy};
        while(flightState[0] < d) {
            rk4StepFlight(flightState, dt);
            if (flightState[1] < 0) {
                return -167.41;
            }
        }

        return flightState[1] - h2;
    }
    // rk4 (generated by ai, too lzy to write this all up)
    static void rk4StepHood(double[] s, double dt) {
        double[] k1 = dHood(s[0], s[1]);
        double[] k2 = dHood(s[0] + k1[0]*dt/2, s[1] + k1[1]*dt/2);
        double[] k3 = dHood(s[0] + k2[0]*dt/2, s[1] + k2[1]*dt/2);
        double[] k4 = dHood(s[0] + k3[0]*dt, s[1] + k3[1]*dt);
        s[0] += dt/6*(k1[0]+2*k2[0]+2*k3[0]+k4[0]);
        s[1] += dt/6*(k1[1]+2*k2[1]+2*k3[1]+k4[1]);
    }

    static double[] dHood(double t, double v) {
        double F_drag = 0.5 * constantsTrajectory.rho * constantsTrajectory.A * constantsTrajectory.Cd * v * v;
        double F_lift = 0.5 * constantsTrajectory.rho * constantsTrajectory.A * constantsTrajectory.Cl * v * v;
        double N = (constantsTrajectory.m * v * v / constantsTrajectory.rHood) + (constantsTrajectory.m * constantsTrajectory.g * Math.cos(t)) - F_lift;
        if (N < 0) N = 0;
        return new double[]{v/constantsTrajectory.rHood, (-constantsTrajectory.m*constantsTrajectory.g*Math.sin(t) - F_drag - constantsTrajectory.mu*N)/constantsTrajectory.m};
    }

    static void rk4StepFlight(double[] s, double dt) {
        double[] k1 = dFlight(s[2], s[3]);
        double[] k2 = dFlight(s[2] + k1[2]*dt/2, s[3] + k1[3]*dt/2);
        double[] k3 = dFlight(s[2] + k2[2]*dt/2, s[3] + k2[3]*dt/2);
        double[] k4 = dFlight(s[2] + k3[2]*dt, s[3] + k3[3]*dt);
        s[0] += dt/6*(k1[0]+2*k2[0]+2*k3[0]+k4[0]);
        s[1] += dt/6*(k1[1]+2*k2[1]+2*k3[1]+k4[1]);
        s[2] += dt/6*(k1[2]+2*k2[2]+2*k3[2]+k4[2]);
        s[3] += dt/6*(k1[3]+2*k2[3]+2*k3[3]+k4[3]);
    }

    static double[] dFlight(double vx, double vy) {
        double v = Math.sqrt(vx*vx + vy*vy);
        double F = 0.5 * constantsTrajectory.rho * constantsTrajectory.A * v / constantsTrajectory.m;
        return new double[]{vx, vy, -F*(constantsTrajectory.Cd*vx + constantsTrajectory.Cl*vy), -constantsTrajectory.g - F*(constantsTrajectory.Cd*vy - constantsTrajectory.Cl*vx)};
    }
}